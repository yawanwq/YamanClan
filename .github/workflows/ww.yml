name: Build Plugin

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Compile Plugin
        run: |
          mkdir -p out/classes
          javac -classpath paper-1.21.4.jar -d out/classes $(find src -name "*.java")
          jar cvf YamanClan.jar -C out/classes . plugin.yml

      - name: Upload jar
        uses: actions/upload-artifact@v3
        with:
          name: YamanClan
          path: YamanClan.jar
